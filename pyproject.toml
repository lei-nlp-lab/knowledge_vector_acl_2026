[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "knowledge-vector-reasoning"
version = "0.1.0"
description = "Code framework for logical-reasoning knowledge vectors."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
